// Widget test - iskelet testi
import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('App smoke test', (WidgetTester tester) async {
    // Ana widget testi main.dart'ın ProviderScope + EasyLocalization
    // gerektirdiğinden burada placeholder olarak bırakılmıştır.
    expect(true, isTrue);
  });
}
